
<%= form_for(@abstract_object) do |f| %>
  <% if @abstract_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@abstract_object.errors.count, "error") %> prohibited this abstract_object from being saved:</h2>

      <ul>
      <% @abstract_object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
