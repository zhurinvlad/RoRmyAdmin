<%= form_for(@db_connect) do |f| %>
  <% if @db_connect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@db_connect.errors.count, "error") %> prohibited this db_connect from being saved:</h2>

      <ul>
      <% @db_connect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :data_base %><br>
    <%= f.text_field :data_base %>
  </div>
  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
